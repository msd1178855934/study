<view class="container">
    <view wx:for="{{images}}" wx:key="index">
        <image class="pic item-{{index}}" mode="widthFix" src="{{item.show?item.src:placeholder}}" style="height:{{item.imageheight}}rpx;"></image>
    </view>
</view>
